# Built by the build-*.sh scripts next to this file. ~117 MB combined, and
# reproducible in about a minute, so they are not worth versioning.
*.xcframework/
# Built once by build-libssh2.sh and cached.
openssl/
# Go module and build caches. Kept beside the vendored Go source so building
# Termther never writes dependencies into ~/go or ~/Library/Caches.
.go/
